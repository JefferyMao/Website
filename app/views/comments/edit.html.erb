<%= form_with(model: [@travel, @comment], url: travel_comment_path(@comment.travel, @comment), local: true) do |f| %>
    <%= f.text_area :content %>
    <%= button_tag type: "submit" do %>
      <i class="far fa-comments"></i> コメントする
    <% end %>
  <% end %>
end